class ServerConfig {
  static const host = 'https://waytech.mbastin.ir';

  static const GetStations = host + '/api/station/';
  static const PathFinder = host + '/api/path-finder/';
  static const IncomingLines = host + '/api/incoming-lines/';
}